.venv
__pycache__
output/
.git
*.pyc

# Real personal data - never belongs in the image, only ever mounted in at runtime via the
# /config and /data volumes. See .gitignore for why config/ isn't tracked either.
config/
data/
